The Estimation Problem
In modulation schemes that use periodic basis functions, si(t) is periodic and the bit duration T is an integer
multiple  of  the  period  of  the  basis  function(s);  hence,  ∫
2(t)dt  is  constant.  By  contrast,  chaotic  signals  are
inherently nonperiodic, so ∫