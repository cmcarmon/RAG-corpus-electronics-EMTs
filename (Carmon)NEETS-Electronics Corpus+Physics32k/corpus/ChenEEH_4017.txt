1. Hard-wired bit serial architecture supported in Ca-
thedral I is suitable to a low-sample rate ﬁlter in audio,
speech, and telecommunication applications. Because
signals are processed bit by bit,
least signiﬁcant bit
(LSB) ﬁrst, the amount of logic is independent of the
word length.