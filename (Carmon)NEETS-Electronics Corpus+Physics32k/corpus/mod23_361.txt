 Q-9. Which method of digital data encoding does NOT return the tape to neutral between pulses but,
instead, saturates the tape positively or negatively as the incoming data changes between zero
and one?
