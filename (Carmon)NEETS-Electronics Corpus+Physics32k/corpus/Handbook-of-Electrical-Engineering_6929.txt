In another classiﬁcation, a stochastic process X(t) is deterministic whenever an entire sample function can
be determined from an initial segment {x t: t £ t1} of X(t). Otherwise, it is nondeterministic [see Brown, 1983,
p. 79; or Gardner, 1990, p. 304].