Dividing by 2d and taking the limit as d ﬁ 0 yields (1/2)SXX(f0) + (1/2)SXX(–f0), which becomes SXX(f0) when
we use the fact that psdf’s are even and real functions (because they are the Fourier transforms of autocorrelation
functions, which are even and real).