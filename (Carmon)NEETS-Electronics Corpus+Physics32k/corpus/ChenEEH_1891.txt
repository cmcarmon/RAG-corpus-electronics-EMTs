A fault is active when it produces an error and is dormant
otherwise. An active fault is an external fault or a previously
dormant one that was activated by the computation of the
system. An error is latent prior to its detection and is detected
if it is recognized by some mechanism.