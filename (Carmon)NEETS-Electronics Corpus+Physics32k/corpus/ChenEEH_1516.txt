the vertical bar, called a pipe symbol, says to take the output
from sort and use it as the input to head. A collection of
commands connected by pipe symbols, called a pipeline,
may arbitrarily contain many commands. A four-component
pipeline is shown by the following two examples. The ﬁrst one
is written as: