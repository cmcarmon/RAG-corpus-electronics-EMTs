Note that as the consistency set S(y) reduces to a single element, the experiment operator tends to be invertible.
Since the identiﬁcation error is deﬁned in a worst-case sense, its convergence is uniform with respect to the a
priori sets ᏺ and S.