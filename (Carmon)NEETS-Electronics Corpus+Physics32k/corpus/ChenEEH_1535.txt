The MS-DOS ﬁle system supports hierarchical directories,
absolute and relative path names, and many of the same system
calls as the UNIX ﬁle system. I/O is done using special ﬁles,
both block and character. It also has the capability to handle
primitive shell scripts, called batch ﬁles.