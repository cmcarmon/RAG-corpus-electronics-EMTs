Using a hard-wired algorithm and random number generation, it is possible to provide high-level security.
The reader and the card use a mutual authentication and a stream ciphering. Such system must also provide
anticollision management to operate with multiple cards in the communication ﬁeld.