• Given a set of reconstruction points, the distortion is minimized by mapping a source random variable
X to the representation Xˆ (w) that is closest to it (in distortion). The set of regions deﬁned by this mapping
is called a Voronoi or Dirichlet partition deﬁned by the reconstruction points.