The microprocessor in your computer works in much the same way. It 
uses lots of teeny digital circuits — known among computer types as 
 digital logic — to process simple functions similar to “2 + 2 = 4.” Then the 
logic  combines the outputs of those functions by applying rules similar 