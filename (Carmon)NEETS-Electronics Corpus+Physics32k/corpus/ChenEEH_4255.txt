Routers in a LAN are used to connect LANs and the Inter-
net. The addressing scope of the router is at the network layer
(e.g., IP). A router makes a decision about how to route the
packets. The routing protocol can be either internal within a
domain network or external between two domain networks.