Gaussian and Markov Processes
A process X(t) is deﬁned to be Gaussian if for every possible ﬁnite set {t1, . . ., tn} of times, the rv’s X(t1), . . .,
X(tn) are jointly Gaussian, which means that every linear combination Z = a1X(t1) + . . . + anX(tn) is a Gaussian
rv, deﬁned by the Gaussian pdf