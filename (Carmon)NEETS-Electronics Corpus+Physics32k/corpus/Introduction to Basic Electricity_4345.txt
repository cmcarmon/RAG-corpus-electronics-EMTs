A NoR gate is a combination of an inverter and an 
OR gate. Its name derives from its NOT-OR func-
tion. Like the NAND gate, the NOR gate can also be 
used to construct an AND gate, an OR gate, and an 
inverter.