In the above statement, the SELECT clause speciﬁes the attribute names that are to be retrieved, FROM gives
the list of the relations involved, and WHERE is a Boolean predicate that completely speciﬁes the tuples to be
retrieved.