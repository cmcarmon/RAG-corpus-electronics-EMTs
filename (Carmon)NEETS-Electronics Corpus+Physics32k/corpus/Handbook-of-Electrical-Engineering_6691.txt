The ISO/OSI protocol suite is in a constant state of revision as new experience reveals the need for additional
capabilities and ﬂexibility. Some of this additional ﬂexibility and functionality is being provided through the
adaption sublayers
,
 which enhance the capabilities of a given layer so that it can use the services
use of so-called 
of a lower layer with which it was not speciﬁcally designed for compatibility.