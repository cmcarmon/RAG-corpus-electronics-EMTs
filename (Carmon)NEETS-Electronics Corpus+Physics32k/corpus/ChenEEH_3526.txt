where a and b are scalars (real or complex numbers depending
on the mapping T). The above rule can be extended to sum of
a ﬁnite set of inputs by induction. We will assume that the
system allows the extension of this property to a countably
inﬁnite sum of inputs.