The function  f(q,j) is normally called the array factor or the array polynomial. Thus, one can ﬁnd E tot by just
multiplying the individual element’s electric ﬁeld pattern, K(q,j), by the array factor,  f (q,j). This process is
often referred to as pattern multiplication.