conversion. Figure 44-27 shows how a shift register 
can be loaded by a parallel input. For parallel opera-
tions, the input data are preset into the shift register. 
Once the data are in the shift register, they can be 
shifted out serially, as discussed earlier.