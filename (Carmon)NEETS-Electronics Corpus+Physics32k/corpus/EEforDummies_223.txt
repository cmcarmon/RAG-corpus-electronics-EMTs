Take a look at the series‐parallel circuit in Figure 4-9. Note the three parallel 
branches, each containing a switch in series with a resistor and an LED. The 
switches are represented by the symbols at the top of each branch.