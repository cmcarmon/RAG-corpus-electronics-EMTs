If all the elements of the 2-D array have excitation currents equal in magnitude and phase (all the amn are
equal and az = ay = 0), then the array will be a broadside array and will have a normalized array factor given by