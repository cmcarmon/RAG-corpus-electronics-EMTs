9.4.2 Deﬁnition of PZ Nets
Let Z ¼ (ZP, ZT, ZI) be a collection of Z schemas. If we let z be
a Z schema, we use name(z), sig(z), and prop(z) to denote the
name, the signature part (as a typed set of mappings), and
property part of z, respectively in the sequel. The Z schemas in
Z satisfy the following conditions: