A user can interact with the operating system through a special
process, the command interpreter using a command lan-
guage. A command language can be either very simple in the
early days or as powerful as a regular programming language.
Most UNIX systems offer one or more command interpreter
with a name called shell or its variations. Unfortunately, the