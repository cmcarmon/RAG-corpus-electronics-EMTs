different classes are grouped together in one high-level class
called a superclass, with specialization in subclasses. The class
structure makes it easier to reuse modular portions of the
software for multiple applications because changes can be
localized in object classes.