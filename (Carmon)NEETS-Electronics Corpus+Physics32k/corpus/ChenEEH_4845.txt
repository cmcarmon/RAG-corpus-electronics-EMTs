Consider the real-time operation of a complex control
system where different components are connected via a net-
work. There are three places where the time delays occur: the
data transfer over the network, the software applications
running on the processors, and the middleware that inte-
grates the components.