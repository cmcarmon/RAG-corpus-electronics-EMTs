Obviously, this approach is only applicable for signals with ﬁnite alphabet since otherwise the set Y(z) is empty
with probability one. For signals with continuous pdf’s, the approach can be applied only if those signals are
appropriately quantized.