Files can be grouped together for convenience in directories.
Directories are stored as ﬁles and, to a large extent, can be
treated like ﬁles. They are protected with the same 9 bits as
ﬁles, except that the 3 execute bits grant or deny permission to
search the directory rather than execute it.