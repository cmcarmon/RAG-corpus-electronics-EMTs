▶✓ NAND gate: This function behaves like an AND gate followed by an 
inverter (hence the NAND, which means NOT AND). It produces an 
output of 0 only if all of its inputs are 1. If any input is 0, the output is 1.
▶✓ OR gate: The output is 1 if at least one of its inputs is 1. It produces an 