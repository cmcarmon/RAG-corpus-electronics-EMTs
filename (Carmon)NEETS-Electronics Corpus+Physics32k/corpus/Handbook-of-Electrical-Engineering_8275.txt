stuck-at fault
,
 which is modeled by having a line segment
stuck at logic 0 or 1 (stuck-at 1 or stuck-at 0). One may consider single or multiple stuck-at faults and Fig. 85.3
 while the faulty functions, under
shows an example for a simple circuit. The fault-free function is shown as 