-version programming was developed to allow certain design ﬂaws in software modules to
-version programming is to design and code
be tolerated [Chen and Avizienis, 1978]. The basic concept of 
 modules
the software module 
is designed and coded by a separate group of programmers. Each group designs the software from the same
modules performs the same function. However, it is hoped that
set of speciﬁcations such that each of the 
  designs  independently,  the  same  mistakes  will  not  be  made  by  the  different  groups.
by  performing  the 
Therefore, when a fault occurs, the fault will either not occur in all modules or it will occur differently in each
module, so that the results generated by the modules will differ. Assuming that the faults are independent the
approach can tolerate (