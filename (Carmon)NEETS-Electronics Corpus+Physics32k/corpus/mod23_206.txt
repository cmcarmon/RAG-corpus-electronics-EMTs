This network takes the input signals from the speed select network and the capstan speed monitor, 
compares the two signals, and decides if the capstan is at the right speed. If not, it tells the speed 
correction circuit. 
