Microcommand: A small bit ﬁeld indicating if a gate is open or closed, if a function is enabled or not, if a
control path is active or not, etc. A microcommand is therefore the speciﬁcation of some action within
the control structure of a CPU.