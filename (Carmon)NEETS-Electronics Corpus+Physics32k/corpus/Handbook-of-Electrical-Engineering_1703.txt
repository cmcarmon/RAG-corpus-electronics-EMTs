and converting it to an electrical signal. The quantization process then assigns a number to the electrical signal;
 of numbers, is the digital representation of the image. Each spatial location in the
the result, which is a 
(or pel). The size of the
image (or grid) to which a number is assigned is known as a 
 512,
sampling grid is usually given by the number of pixels on each side of the grid, e.g., 256 
488 