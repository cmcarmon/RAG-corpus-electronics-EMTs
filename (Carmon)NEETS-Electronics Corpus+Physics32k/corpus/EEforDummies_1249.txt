from an earlier stage (for instance, if you use a tone generator or other 
device as the input signal source). The parts list says to use a 10 μF 
capacitor for C1, but you may try values as low as 0.1 μF (or try eliminat-
ing C1 and observe what happens).