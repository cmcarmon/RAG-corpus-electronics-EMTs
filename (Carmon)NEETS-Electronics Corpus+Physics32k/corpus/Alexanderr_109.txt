7. Two elements are in series when they are connected sequentially,
end  to  end.  When  elements  are  in  series,  the  same  current  ﬂows
through them 
. They are in parallel if they are connected
to the same two nodes. Elements in parallel always have the same
voltage across them (