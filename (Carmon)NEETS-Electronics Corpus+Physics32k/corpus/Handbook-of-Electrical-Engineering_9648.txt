and  debugging  at  the  application  layer  is  inherently  simpler  than  programming  at  the  OS  layer.  The  beneﬁt
here is to the OS designer and implementors who can now write and debug OS code faster and easier than
before. This beneﬁts the user by having an operating system that is more reliable.