Once a floppy disk is formatted, the computer uses the disk's side number, a track number, and a 
sector number (together) as an address. It's this address that locates where on the disk the computer will 
store the data. 
