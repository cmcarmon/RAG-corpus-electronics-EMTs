The pulse Doppler radar usually uses ﬁlter banks, which are
implemented with a discrete Fourier transform rather than
delay cancellers to remove the clutter. The improvement factor
is a function of the size of the Fourier transform and the
window function used.