Figure 45-2 shows a decimal-to-binary priority  
encoder. The priority function means that if two keys 
are pressed simultaneously, the encoder produces a 
BCD output corresponding to the highest-order deci-
mal digit appearing on the input. For example, if both 