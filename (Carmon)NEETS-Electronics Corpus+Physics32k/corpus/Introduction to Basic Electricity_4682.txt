(decreased by one), shifted right (one position), or 
shifted left (one position). The accumulator is the 
same size as the memory word; the memory word is 32 
bits wide, and the accumulator is also 32 bits wide in a 
32-bit microprocessor.