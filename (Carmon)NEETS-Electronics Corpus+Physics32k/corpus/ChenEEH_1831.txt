(3) Trafﬁc-driven label assignment. This assignment
combines the advantages of the above two methods.
LSPs are set up only when the LSR identiﬁes trafﬁc
patterns that justify the setup of an LSP. Those that
are identiﬁed as not needing an established LSP are
routed using the normal routing method.