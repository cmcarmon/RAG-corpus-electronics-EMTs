The ﬁnal step in image encoding is entropy coding. Here we convert the stream of prototype feature vectors
to a binary stream of 0’s and 1’s. Ideally, we would like to perform this conversion in a manner that yields the
minimum average number of binary digits per prototype feature vector.