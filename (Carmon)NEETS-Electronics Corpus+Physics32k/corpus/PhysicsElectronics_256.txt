(3.1)
Figure 3.1 shows some ideal transfer functions. If H(jω) = H ≡ A is a real constant
then we call the network an ideal ampliﬁer. If H(jω) = Θ(j(ω − ω0)) is a heavyside step
function we refer to the circuit as an ideal low-pass ﬁlter, and if H(jω) = 1 − Θ(j(ω − ω0))