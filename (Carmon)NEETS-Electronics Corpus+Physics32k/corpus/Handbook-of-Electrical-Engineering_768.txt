and * z *  >  r
0  depend  on  the  actual
transformed functions; in factor z, the inverse sequence would begin at n = 0. Thus, we use a modiﬁed partial-
fraction expansion whose terms have this extra z-factor.