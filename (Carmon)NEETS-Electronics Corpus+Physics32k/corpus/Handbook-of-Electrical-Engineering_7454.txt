q
), 
t
/2 Hz. A computer
t
(
(
A
), 
)  with  a  pair  of  LPE  signals  will  require  far  less  processor  time  since  the  LPE
t
x
(
simulation  that  replaces 
 samples per second. It is cumbersome to work with two
waveforms can be sampled at 
signals rather than one signal. A more mathematically elegant LPE expansion is