 [Haykin, 1994]. The optimal weights are
)  and  the  cross-correlation
) between the input and the desired response. The analytical solution is equivalent to a search for the
) using gradient descent, where the weights at each iteration