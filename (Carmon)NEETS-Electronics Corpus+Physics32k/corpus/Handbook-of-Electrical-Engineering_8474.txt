In  the  RC  number  system  this  is  equivalent  to  passing  the  operands  through  an  adder  and  discarding  the
carry-out of the most signiﬁcant position of the adder which is equivalent to performing the modulo addition
(given that C = r n).