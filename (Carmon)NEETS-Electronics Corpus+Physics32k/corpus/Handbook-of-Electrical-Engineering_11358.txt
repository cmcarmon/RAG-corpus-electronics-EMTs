 plane, its
 [Castleman, 1979]. Thus as we rotate the source and
transform will be similarly rotated through an angle 
)  can  be  Fourier  transformed  to
detector  around  the  object,  each  projected  density  function  detected 
),
provide one radial line of the two-dimensional Fourier transform of the desired reconstructed image, 
  form  a  set  of
where 
), the two-dimensional Fourier
spokes in the transform domain which can be interpolated to estimate 
transform of the image in rectangular coordinates. The image can then be recovered by inverse transformation
of 