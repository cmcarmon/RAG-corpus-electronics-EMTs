For a parallel processor, the degree of concurrency is called the degree of parallelism. A similar discussion
can be used to deﬁne the average hardware utilization of a parallel processor. The maximum parallelism is then
P
m