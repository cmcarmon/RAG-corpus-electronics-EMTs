but not both — of the inputs is 1; otherwise, it produces an output of 0. 
All XOR gates have two inputs, but multiple XOR gates can be cascaded 
together to create the effect of XORing multiple inputs.