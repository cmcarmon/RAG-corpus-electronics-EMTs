Figure 5.27 shows a coherent (synchronization-based) receiver using binary CSK modulation with two basis
functions g1(t) and g2(t). Synchronization circuits at the receiver attempt to reproduce the basis functions, given
the received noisy sample function ri(t) = si(t) + n(t).