Arrays
Arrays permit the construction of simple function spaces, I ﬁ A. The domain I, a scalar type—in some languages
restricted to a subset {0, 1, ..., n} of the integers—is called the index set of the array; A is an arbitrary type. In
Pascal, the mathematical notation f ˛ I ﬁ A assumes the form: