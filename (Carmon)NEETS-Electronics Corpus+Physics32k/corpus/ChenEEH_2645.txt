For an inhomogeneous object, we can follow the basic
approach outlined above to derive a so-called volume-surface
integral equation (Jin et al., 1988), which contains an integral
over Vo in addition to the integral over Go. As a result, the
unknown function is distributed over Vo and Go.