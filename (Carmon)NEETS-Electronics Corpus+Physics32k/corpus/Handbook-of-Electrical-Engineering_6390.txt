x
n–k–
1
k
 data digits are shifted in one at a time at the input with the switch 
s
 represents a one-digit delay. As the data digits move through the encoder,
  digits  of  code  word  are  the  data  digits
k
) register, all the registers contain the
n – k
n – k
 parity-check digits are shifted out