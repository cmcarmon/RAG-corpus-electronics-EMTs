• Signal duration. The sum extends over the signal’s duration, which must be ﬁnite to compute the
It is exceedingly diﬃcult to store an inﬁnite-length signal in any case, so we’ll
• Continuous frequency. Subtler than the signal duration issue is the fact that the frequency variable