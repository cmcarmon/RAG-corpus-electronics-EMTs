However, equation 8.4 still does not allow us to solve for cn—
we have to convert equation 8.4 into a matrix equation. To this
end, we choose a set of functions w1(r), w2(r), . . ., wN (r) and
multiply equation 8.4 by these functions individually. We then
integrate to obtain: