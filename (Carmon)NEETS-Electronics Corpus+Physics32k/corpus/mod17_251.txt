Figure 2-20 is a block diagram of a superheterodyne receiver capable of receiving and demodulating
a cw signal. The bfo heterodynes at the detector and produces an af output. The detector (second detector)
is used primarily because the mixer (first detector) is normally used as the source of agc.
