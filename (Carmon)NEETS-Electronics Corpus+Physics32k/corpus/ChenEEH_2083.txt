variables in TC(t) for any transition t, let S(v) denote
the sort (or type) of a variable n, and let S(V ) denote the
set of the sorts of the variables in the sorted set V. Then,
the following constraints are satisﬁed for any p 2 P:
(1) AA(p, t )  Var(t ) and AA(t, p)  Var(t ), where